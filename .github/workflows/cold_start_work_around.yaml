name: Send GET Request Every 5 Minutes and Manually

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  send-request:
    runs-on: ubuntu-latest

    steps:
      - name: Send GET Request
        run: |
          curl -X GET 'https://charlie-glass-admin-api.vercel.app/'